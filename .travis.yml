language: python
python:
  - "3.7"
cache: pip
before_install:
  - python3 --version
  - pip3 install -U pip
  - pip3 install -U pytest
install:
  - pip install -r requirements.txt
script:
  - python -m pytests